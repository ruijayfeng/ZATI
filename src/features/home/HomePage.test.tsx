import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { describe,expect,it } from 'vitest'; import { HomePage } from './HomePage';
describe('home page',()=>{it('offers truthful quick and standard paths with one primary action',()=>{render(<MemoryRouter><HomePage/></MemoryRouter>);expect(screen.getByRole('heading',{name:'以选择看见自己'})).toBeInTheDocument();expect(screen.getByRole('link',{name:'开启本命测试'})).toHaveAttribute('href','/test/quick');expect(screen.getByText('24题 · 约3分钟')).toBeInTheDocument();expect(screen.getByText('56题 · 正式报告')).toBeInTheDocument();expect(document.querySelectorAll('.command:not(.secondary)')).toHaveLength(1);});});
