import { render,screen } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { MemoryRouter,Route,Routes } from 'react-router-dom'; import { beforeEach,describe,expect,it } from 'vitest'; import { RevealPage } from './RevealPage';
describe('reveal page',()=>{beforeEach(()=>localStorage.clear());it('recovers when no result exists',()=>{render(<MemoryRouter><RevealPage/></MemoryRouter>);expect(screen.getByRole('link',{name:'重新测试'})).toHaveAttribute('href','/test/quick');});it('offers an accessible skip action',async()=>{localStorage.setItem('zati:result',JSON.stringify({version:1,mode:'quick',code:'AVLH',scores:{AR:68,VS:74,LI:59,DH:81},boundaries:{AR:false,VS:false,LI:false,DH:false},completedAt:new Date().toISOString()}));render(<MemoryRouter initialEntries={['/reveal']}><Routes><Route path="/reveal" element={<RevealPage/>}/><Route path="/result" element={<h1>结果</h1>}/></Routes></MemoryRouter>);await userEvent.click(screen.getByRole('button',{name:'跳过动画'}));expect(screen.getByRole('heading',{name:'结果'})).toBeInTheDocument();});});
