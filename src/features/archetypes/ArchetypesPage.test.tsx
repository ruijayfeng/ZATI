import { render,screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { describe,expect,it } from 'vitest'; import { ArchetypesPage } from './ArchetypesPage';
describe('archetype page',()=>{it('shows all sixteen identities grouped by domain',()=>{render(<MemoryRouter><ArchetypesPage/></MemoryRouter>);expect(screen.getAllByRole('article')).toHaveLength(16);expect(screen.getByRole('heading',{name:'十六张本命人格牌'})).toBeInTheDocument();for(const name of ['破局星域','定盘星域','造浪星域','稳场星域'])expect(screen.getByRole('heading',{name})).toBeInTheDocument();});});
